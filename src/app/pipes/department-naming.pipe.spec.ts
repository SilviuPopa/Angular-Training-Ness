import { DepartmentNamingPipe } from './department-naming.pipe';

describe('DepartmentNamingPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DepartmentNamingPipe();
    expect(pipe).toBeTruthy();
  });
});
