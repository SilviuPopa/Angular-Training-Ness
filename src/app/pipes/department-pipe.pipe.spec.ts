import { DepartmentPipe } from './department-filter.pipe';

describe('DepartmentPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DepartmentPipe();
    expect(pipe).toBeTruthy();
  });
});
