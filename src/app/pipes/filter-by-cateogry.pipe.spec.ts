import { FilterByCateogryPipe } from './filter-by-cateogry.pipe';

describe('FilterByCateogryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByCateogryPipe();
    expect(pipe).toBeTruthy();
  });
});
